package arrays;

public class SquareElements {
    public static void main (String [] args){
        int [] arr = {2,4,6,8};
        int last  = arr[arr.length -1];
        System.out.println("Square :" + (last * last));
    }
}
